export type ScenarioSummary = {
  id: string;
  period: string;
  region: string;
  title: string;
  description: string;
};

export type SimulationStep = {
  id: string;
  title: string;
  description: string;
};
